  TextView aiIntro=text("Dokumentumokból automatikus kitöltés",13,true);aiIntro.setTextColor(roseDark);x.addView(aiIntro);
        TextView aiHint=text("PDF, Word vagy kép is csatolható: munkaszerződés, megbízási szerződés, munkaköri leírás, bér/járulék-kimutatás és táppénzes igazolás. Több dokumentumot is hozzáadhatsz ugyanahhoz a jogviszonyhoz.",12,false);aiHint.setTextColor(muted);x.addView(aiHint);
        Button document=secondaryButton("📎 Dokumentum / kép hozzáadása AI-val");x.addView(document);
        state.aiStatus=text(SecureStore.getApiKey(this).isEmpty()?"OpenAI API-kulcs nincs beállítva.":"OpenAI készen áll a dokumentum vagy kép feldolgozására.",11,false);state.aiStatus.setTextColor(SecureStore.getApiKey(this).isEmpty()?Color.rgb(173,91,67):Color.rgb(65,126,94));x.addView(state.aiStatus);
        state.documentLog=text("Még nincs feldolgozott dokumentum.",11,false);state.documentLog.setTextColor(muted);x.addView(state.documentLog);

        TextView manual=label("Jogviszony adatai");manual.setPadding(0,dp(12),0,0);x.addView(manual);
        state.relationType=new Spinner(this); String[] types={"Munkaviszony","Normál megbízás","Tartós megbízás","Egyéni vállalkozó","Egyéb"};
        ArrayAdapter<String> adapter=new ArrayAdapter<>(this,android.R.layout.simple_spinner_item,types);adapter.setDropDownViewResource(android.R.layout.simple_spinner_dropdown_item);state.relationType.setAdapter(adapter);x.addView(state.relationType);
        state.employer=input("Munkáltató / megbízó / jogviszony neve"); state.start=input("Kezdet ÉÉÉÉ-HH-NN"); state.end=input("Vége ÉÉÉÉ-HH-NN (üres = fennáll)");
        state.gross=input("Havi bruttó / járulékalapot képező jövedelem Ft");state.gross.setInputType(InputType.TYPE_CLASS_NUMBER);
        state.sick=input("Táppénzes / keresőképtelen napok");state.sick.setInputType(InputType.TYPE_CLASS_NUMBER);
        state.unpaid=input("Nem biztosított / fizetés nélküli napok");state.unpaid.setInputType(InputType.TYPE_CLASS_NUMBER); state.insured=check("Biztosítási jogviszony",true);
        x.addView(state.employer);x.addView(state.start);x.addView(state.end);x.addView(state.gross);x.addView(state.sick);x.addView(state.unpaid);x.addView(state.insured);
        TextView mandateHelp=text("Normál megbízásnál az app a biztosítottságot csak becsüli a járulékalapot képező havi jövedelem és/vagy feltöltött TB-bizonyíték alapján. 2026-tól a tartós megbízás külön biztosítotti kategória.",11,false);mandateHelp.setTextColor(muted);x.addView(mandateHelp);

        if(existing!=null){state.employer.setText(existing.employer);state.start.setText(existing.start==null?"":existing.start.toString());state.end.setText(existing.end==null?"":existing.end.toString());state.gross.setText(String.valueOf(existing.monthlyGross));state.sick.setText(String.valueOf(existing.sickPayDays));state.unpaid.setText(String.valueOf(existing.unpaidDays));state.insured.setChecked(existing.insured);selectSpinner(state.relationType,existing.relationType);state.evidence.addAll(existing.documentEvidence);updateDocumentLog(state);}

        AlertDialog dlg=new AlertDialog.Builder(this).setTitle(existing==null?"Új munka / jogviszony":"Munka / jogviszony szerkesztése").setView(scroll).setPositiveButton(existing==null?"Hozzáadás":"Módosítás mentése",null).setNegativeButton("Mégse",null).create();state.dialog=dlg;activeJobDialog=state;
        document.setOnClickListener(v->{if(SecureStore.getApiKey(this).isEmpty()){new AlertDialog.Builder(this).setTitle("OpenAI API-kulcs szükséges").setMessage("A dokumentumok és képek automatikus feldolgozásához előbb add meg az OpenAI API-kulcsot a Beállításokban. Kézzel továbbra is kitölthető minden mező.").setPositiveButton("Beállítások",(d,w)->showSettings()).setNegativeButton("Mégse",null).show();return;}Intent i=new Intent(Intent.ACTION_OPEN_DOCUMENT);i.setType("*/*");i.putExtra(Intent.EXTRA_MIME_TYPES,new String[]{"application/pdf","image/jpeg","image/png","image/webp","application/msword","application/vnd.openxmlformats-officedocument.wordprocessingml.document","text/plain"});i.addCategory(Intent.CATEGORY_OPENABLE);startActivityForResult(i,REQ_JOB_DOCUMENT);});
        dlg.setOnDismissListener(d->{if(activeJobDialog==state)activeJobDialog=null;});
        dlg.setOnShowListener(v->dlg.getButton(AlertDialog.BUTTON_POSITIVE).setOnClickListener(w->{try{String rel=String.valueOf(state.relationType.getSelectedItem());LocalDate st=LocalDate.parse(state.start.getText().toString().trim());long amount=state.gross.getText().toString().trim().isEmpty()?0:Long.parseLong(state.gross.getText().toString().trim());boolean ins=state.insured.isChecked();if("Tartós megbízás".equals(rel))ins=true;if("Normál megbízás".equals(rel)&&amount>0&&!state.aiStatus.getText().toString().contains("TB/biztosítás igazolva")){ins=CalculatorEngine.normalMandateLikelyInsured(st,amount);}EmploymentPeriod ep=new EmploymentPeriod(state.employer.getText().toString().trim(),rel,st,state.end.getText().toString().trim().isEmpty()?null:LocalDate.parse(state.end.getText().toString().trim()),amount,ins,parseInt(state.unpaid),parseInt(state.sick),state.evidence);if(ep.employer.isEmpty()){toast("Add meg a munkáltató, megbízó vagy jogviszony nevét.");return;}if(state.editIndex>=0&&state.editIndex<jobs.size())jobs.set(state.editIndex,ep);else jobs.add(ep);renderJobs();dlg.dismiss();}catch(Exception e){toast("Hibás vagy hiányzó dátum/összeg. Dátumpélda: 2026-08-23");}}));dlg.show();
    }

    private void analyzeDocumentInt